export default function Home() {
  return (
    <main className="flex flex-1 flex-col items-center justify-center px-6 py-24 text-center">
      <p className="text-sm font-medium uppercase tracking-widest text-[var(--muted)]">Nomi · Phase 0</p>
      <h1 className="mt-4 max-w-2xl text-4xl font-semibold tracking-tight sm:text-5xl">
        Tell it once. Pick up where life left off.
      </h1>
      <p className="mt-4 max-w-xl text-base text-[var(--muted)]">
        The assistant, memory, research and approval flows are being built. Nothing here is connected yet.
      </p>
    </main>
  );
}
